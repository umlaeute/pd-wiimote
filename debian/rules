#!/usr/bin/make -f

%:
	dh $@

#override_dh_shlibdeps:
#	dpkg-shlibdeps $(CURDIR)/debian/$(PACKAGE)$(pkglibdir)/$(LIBRARY_NAME)/*.pd_linux \
#		-T$(CURDIR)/debian/$(PACKAGE).substvars
#
#override_dh_strip:
#	strip --strip-unneeded $(CURDIR)/debian/$(PACKAGE)$(pkglibdir)/$(LIBRARY_NAME)/*.pd_linux
